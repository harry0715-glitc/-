# 施工人員名冊專案進度記錄

最後更新：2026-06-30

## 已完成
- 後端基本驗證已補上（姓名、職稱、包商、身分證、手機、備註長度）
- 管理帳號固定為 `admin`
- 管理密碼改為由 Apps Script Script Properties 管理，可初始化、登入驗證、修改
- 包商名稱防重複
- 人員資料防重複
  - 同身分證字號擋下
  - 同包商下同姓名 + 同手機擋下
- 表單資料正規化
  - 去除前後空白
  - 合併多餘空白
  - 身分證轉大寫
  - 手機只保留數字
- 刪除包商前會檢查底下是否仍有人員資料
- 敏感資料保護已補齊
  - 公開 `getData()` 預設回傳遮罩後的身分證
  - 公開 `getData()` 現在保留完整手機號碼顯示，不再遮罩電話
  - 管理者登入後，帶管理密碼載入時才會取得完整資料
- 刪除同步規則已補齊
  - 刪除主資料表人員時，會同步刪除包商獨立名冊對應資料
  - 刪除後會重新整理包商名冊中的流水序號
- 前端查詢 / 明細頁顯示已調整
  - 身分證維持遮罩
  - 手機改為顯示完整號碼，保留前導 `0`
  - 進場日期改為中文格式顯示（例如 `2026-06-06` → `2026年6月6日`）
- 已修正 Google Sheet 讀值造成的電話前導 `0` 遺失問題
  - 後端讀表改為優先採用 `getDisplayValues()`
  - 查詢名單電話可正確保留 `09...`
- 已完成前三項前端小升級

  - 查詢頁與管理後台都已加入包商篩選、職稱篩選、排序功能
  - 刪除前改為顯示明確預覽與二次確認，並補上更明確的重複資料提示
- 已新增可測試的名冊整理 helper 模組
  - `src/worker-directory.mjs`
  - `tests/worker-directory.test.mjs`
- 已新增包商名冊列定位 helper，刪除優先改用穩定 `workerId`
  - 新包商名冊隱藏欄位加入 `workerId`
  - 舊 8 欄名冊會自動升級為 9 欄格式
  - 刪除時先用 `workerId`，舊資料才回退舊比對法
- 已新增後端搜尋 / 分頁 helper 與公開查詢頁 API 分頁查詢
  - `src/backend-worker-query.mjs`
  - `tests/backend-worker-query.test.mjs`
  - Query 頁改為呼叫 `searchWorkers`，不再只靠前端整包篩選
- 已補基本自動測試

- 已補 `.env.example`
  - 目前收錄前端使用的 `VITE_APPS_SCRIPT_URL`
- 目前本機 `npm run lint` 通過
- 目前本機 `npm test` 通過（20/20）
- 目前本機 `npm run build` 成功
- 以上變更目前僅完成於本機，尚未 push / deploy 到 GitHub、Apps Script production、Netlify production

## 已撤回 / 不列優先
- 前端匯出 CSV 功能已從目前本機程式碼撤回
- 原因：Google 表單 / 試算表後端本來就有匯出能力，因此不再額外維護前端匯出

## 進行中
- 無

## 尚未進行
- 大資料量效能優化
  - 分頁 API
  - 後端搜尋
  - 更穩定的刪除索引策略
  - 真正資料庫化 / 後端化

## 下次回報進度時的標準格式
1. 已完成
2. 進行中
3. 尚未進行
4. 已撤回 / 不列優先

## 備註
- 之後只要使用者問「施工人員名冊修改進度」，應優先依這份記錄與當前程式碼狀態回報。
- 回報前若這一輪有修改，應先以當前程式碼與測試結果覆核後再更新此檔。
